server: 
   port: 8761

eureka:
   client:
      # 是否 注册到其他Eureka Server实例
      register-with-eureka: false
      # 是否要从其他Eureka Server获取实例
      fetch-registry: false
      service-url:
         defaultZone: http://localhost:8761/eureka/
   